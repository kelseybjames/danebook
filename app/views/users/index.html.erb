<h1>Listing Users</h1>

<table class='table table-striped'>
  <thead>
    <tr>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', user_profile_path(user.id, user.profile) %></td>
        <% if user == current_user %>
          <td><%= link_to 'Edit', edit_user_profile_path(user.id, user.profile) %></td>
          <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% else %>
          <td></td>
          <td></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>