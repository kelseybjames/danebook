<%= render partial: 'photo_form' if current_user == @user %>
<br>
<% unless @photos.nil? %>
  <% @photos.each do |photo| %>
    <%= link_to user_photo_path(@user, photo) do %>
      <%= image_tag photo.image.url(:thumb) %>
    <% end %>
  <% end %>
<% end %>